import { RangeMappingPipe } from './range-mapping.pipe';

describe('RangeMappingPipe', () => {
  it('create an instance', () => {
    const pipe = new RangeMappingPipe();
    expect(pipe).toBeTruthy();
  });
});
